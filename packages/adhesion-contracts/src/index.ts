/**
 * Public API of the `@aiepdf/adhesion-contracts` package.
 *
 * The canonical HTTP contract is `openapi.yaml` in this package; this module
 * exposes the parts of it that the API layer needs at runtime.
 */

export * from './lib/problem.js';
