/**
 * Public API of the `@aiepdf/adhesion-verification` package.
 *
 * Exposes the in-process CAdES verifier that implements the domain
 * `SignatureVerifier` port (`open.item-006`).
 */

export * from './lib/cades-verifier.js';
